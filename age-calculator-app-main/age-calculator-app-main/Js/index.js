document.addEventListener('keydown', (event) => {
    if (event.key === 'Enter') {
        const year = document.getElementById("YEAR")
        if (year.innerHTML.length > 0) {
            if (year.innerHTML.includes([]))
        }
    }
})
